lookup_goenv_dir() {
  local env_dir
  if [ "$GOPATH" ]; then
    for env_dir in $(upsearch_gopath); do
      if [[ "$env_dir" != "/" && "$env_dir" != "$HOME"  ]]; then
        echo "$env_dir"
      fi
  done
  fi
}
